# ---- Ingestion (this step) ----
pymupdf==1.24.10
tiktoken==0.7.0

# ---- Step 2: RAG (embeddings + FAISS) ----
# fastembed (ONNX runtime) instead of sentence-transformers — avoids pulling
# in the full PyTorch library, which alone can exceed free-tier hosting
# memory limits (e.g. Render's 512MB). Same underlying MiniLM model, same
# 384-dim output.
fastembed==0.3.6
faiss-cpu==1.8.0.post1

# ---- Step 3: Knowledge Graph ----
neo4j==5.23.1

# ---- Step 4: LangGraph Agent ----
langgraph==0.2.28

# ---- Step 5: FastAPI + Auth ----
fastapi==0.115.0
uvicorn==0.30.6
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# ---- Step 6: Frontend ----
streamlit==1.38.0